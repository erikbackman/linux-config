#!/bin/env bash

frame=`ratpoison -c curframe`
ratpoison -c "gselect $frame"
exit 0

### OLD
# if [[ "$frame" -eq "1" ]]; then
#     ratpoison -c 'gselect $curframe'
# else
#     ratpoison -c 'gselect default'
# fi
    

